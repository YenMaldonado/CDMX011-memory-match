export const Result = () => {
  let infoTools = document.createElement('div');
  infoTools.className = 'textBox';
  let toolName = document.createTextNode('JS');
  infoTools.appendChild(toolName);
  return Result;
}
